<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mizmer2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Kayit" />
        <activity android:name=".Menu" />
        <activity android:name=".BizKimiz" />
        <activity android:name=".Profil" />
        <activity android:name=".Oneri" />
        <activity android:name=".Asama2" />
        <activity android:name=".Sonuc" />
        <activity android:name=".SlideActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".NavigationDrawerMenu"
            android:label="@string/title_activity_navigation_drawer_menu" />
        <activity android:name=".ExamActivity"></activity>
    </application>

</manifest>